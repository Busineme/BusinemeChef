api:
  run_list:
    - recipe[basics]
    - recipe[busineme::postgresql]
    - recipe[busineme::api]

web:
  run_list:
    - recipe[basics]
    - recipe[busineme::postgresql]
    - recipe[busineme::web]
